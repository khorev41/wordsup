<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/fragment_container_to_change"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/primary"
    tools:context=".fragments.QuizFragment">


    <RelativeLayout
        android:id="@+id/top_button"
        android:layout_width="match_parent"
        android:layout_height="60sp">

        <com.google.android.material.textview.MaterialTextView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginStart="20dp"
            android:fontFamily="@font/roboto_medium"
            android:gravity="center|left"
            android:text="@string/you_can_edit_your_quizzes_here"
            android:textSize="20sp"
            tools:ignore="RtlHardcoded" />

        <ImageView
            android:id="@+id/add_quiz"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:layout_marginTop="15sp"
            android:layout_marginEnd="15sp"
            android:adjustViewBounds="true"
            android:background="@drawable/rounded_border_add"
            android:padding="8sp"
            android:src="@drawable/baseline_add_24"
            tools:ignore="ContentDescription,ImageContrastCheck">

        </ImageView>
    </RelativeLayout>

    <androidx.recyclerview.widget.RecyclerView

        android:id="@+id/recycler_view_quiz"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="20sp"
        android:layout_marginTop="60sp"
        android:orientation="vertical"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        tools:listitem="@layout/quiz_layout">

    </androidx.recyclerview.widget.RecyclerView>
</FrameLayout>
